[package]
name = "fastloops_band"
version = "0.2.0"
edition = "2021"

[lib]
name = "fastloops_band"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
numpy = "0.21"
ndarray = "0.15"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
